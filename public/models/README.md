# 3D Character

The portfolio now uses an original procedural Three.js cabin-crew character in `src/components/Character/Scene.tsx` instead of the original repository's encrypted character asset.

This keeps the 3D experience self-contained and makes it easy to replace the character later with an original rigged `.glb`/`.gltf` model if desired.
